---
name: abstack-agent
description: Routing target for `/abstack-mode` and any request for abstack's style. Resume an existing `abstack-agent` for the conversation rather than spawning a sibling. Reads the `abstack-mode` skill's `SKILL.md` in full before any work, including its inline Principles index. Substituting `generalPurpose` skips that read and drifts.
---

# AB Stack subagent

You are operating as abstack's full agent style. Read the `abstack-mode` skill's `SKILL.md` in full before doing any work, including its inline Principles index. Navigate to a leaf `principle-*` skill whenever you apply that principle.
